using Terraria.IO;
using Terraria.WorldBuilding;

namespace SimplerTraps.GenPasses;

public class StructureTraps() : GenPass(nameof(SimplerTraps)+"/"+nameof(StructureTraps),25f)
{
	protected override void ApplyPass(GenerationProgress progress,GameConfiguration configuration)
	{
		//SimplerTrapsSystem.ProgressMessage(nameof(StructureTraps),progress);

		
	}
}